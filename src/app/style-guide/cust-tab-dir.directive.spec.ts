import { CustTabDirDirective } from './cust-tab-dir.directive';

describe('CustTabDirDirective', () => {
  it('should create an instance', () => {
    const directive = new CustTabDirDirective();
    expect(directive).toBeTruthy();
  });
});
